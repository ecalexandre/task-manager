## Task manager program 🗒️

## About it

This program consists at giving you access to store any tasks but will be stored only when the program is running.
> Note: your data will be deleted when the program closes.

## Features

- Add tasks
- View tasks
- Remove tasks
- Quit the program

## Requirements
- Python 3.13 or Python 3.14

## Installation
1. Clone the repository with `git clone` command on the repository.
2. Create a virtual environment for your script with `python3 -m venv` command.
3. Activate the virtual environment with `source .venv/bin/activate` command

## Usage

- Run your program by simply typing `python3 task-manager.py`.

Then there are going to be 4 options given to you when the program is running:

```
Hello! here is your list of options for your tasks below:
A -Add a task

V -View task list

R -Remove a task

Q -Quit

Enter your option here: 
```

## Example

- If you choose A for Adding a task, this is going to show up:

```
Add your tasks here: 
```
Then you add any task you want

## Project structure
```
task-manager/
|
|__task_manager-jb.py
|__task_manager.py
```

## Author
```
Created by ecalexandre